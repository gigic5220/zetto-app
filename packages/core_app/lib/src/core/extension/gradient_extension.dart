import 'package:flutter/material.dart';

extension GradientExtension on BuildContext {
  // SweepGradient borderSweepGradient({required double animationValue}) => SweepGradient(
  //   startAngle: 0.0,
  //   endAngle: math.pi * 2,
  //   colors: colorTheme.gradientBorder(),
  //   stops: const [0.0 / 9, 1.7 / 9, 3.4 / 9, 5.1 / 9, 6.8 / 9, 8.5 / 9, 1.0],
  //   tileMode: TileMode.clamp,
  //   transform: GradientRotation(animationValue * 2 * math.pi),
  // );

  // SweepGradient chatbotBorderSweepGradient({required double animationValue}) => SweepGradient(
  //   startAngle: 0.0,
  //   endAngle: math.pi * 2,
  //   colors: colorTheme.chatbotGradientBorder(),
  //   stops: const [0.0 / 9, 1.7 / 9, 3.4 / 9, 5.1 / 9, 6.8 / 9, 8.2 / 9, 1.0],
  //   tileMode: TileMode.clamp,
  //   transform: GradientRotation(animationValue * 2 * math.pi),
  // );

  // LinearGradient get circleBgBlueLinearGradient => LinearGradient(
  //   begin: Alignment.topCenter,
  //   end: Alignment.bottomCenter,
  //   stops: [0.5, 1.0],
  //   colors: colorTheme.gradientCircleBgBlue(0.3),
  // );

  // LinearGradient get circleBgGreenLinearGradient => LinearGradient(
  //   begin: Alignment.topCenter,
  //   end: Alignment.bottomCenter,
  //   stops: [0.5, 1.0],
  //   colors: colorTheme.gradientCircleBgGreen(0.3),
  // );
}
