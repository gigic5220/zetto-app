import 'package:flutter/material.dart';

extension ShadowBoxExtension on BoxShadow {
  // BoxShadow get thinSm =>
  //     BoxShadow(color: AppColors.gray900.withValues(alpha: 0.04), offset: Offset.zero, blurRadius: 4, spreadRadius: 0);

  // BoxShadow get thinMdDefault =>
  //     BoxShadow(color: AppColors.gray900.withValues(alpha: 0.04), offset: Offset.zero, blurRadius: 16, spreadRadius: 0);

  // BoxShadow get thinMdTop => BoxShadow(
  //   color: AppColors.gray900.withValues(alpha: 0.04),
  //   offset: const Offset(0, 8),
  //   blurRadius: 16,
  //   spreadRadius: 0,
  // );

  // BoxShadow get thinMdBottom => BoxShadow(
  //   color: AppColors.gray900.withValues(alpha: 0.04),
  //   offset: const Offset(0, -8),
  //   blurRadius: 16,
  //   spreadRadius: 0,
  // );

  // BoxShadow get basicLg =>
  //     BoxShadow(color: AppColors.gray900.withValues(alpha: 0.08), offset: Offset.zero, blurRadius: 32, spreadRadius: 0);

  // BoxShadow get basicMd =>
  //     BoxShadow(color: AppColors.gray900.withValues(alpha: 0.08), offset: Offset.zero, blurRadius: 16, spreadRadius: 0);

  // BoxShadow get basicSmDefault =>
  //     BoxShadow(color: AppColors.gray900.withValues(alpha: 0.08), offset: Offset.zero, blurRadius: 4, spreadRadius: 0);

  // BoxShadow get tickSm =>
  //     BoxShadow(color: AppColors.gray900.withValues(alpha: 0.16), offset: Offset.zero, blurRadius: 4, spreadRadius: 0);

  // BoxShadow get tickXSm =>
  //     BoxShadow(color: AppColors.gray900.withValues(alpha: 0.08), offset: Offset.zero, blurRadius: 4, spreadRadius: 0);

  // BoxShadow get darkMd =>
  //     BoxShadow(color: Color.fromRGBO(252, 246, 230, 0.24), offset: Offset.zero, blurRadius: 16, spreadRadius: 0);
}
